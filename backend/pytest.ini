[pytest]
addopts = -p no:cacheprovider
norecursedirs = pytest_run pytest_runtime openapi_runtime .pytest-tmp .pytest-run .pytest-runtime .openapi-runtime
timeout = 45
timeout_method = thread
filterwarnings =
    ignore:.*urllib3 .* doesn't match a supported version.*:requests.exceptions.RequestsDependencyWarning
